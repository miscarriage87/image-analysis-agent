# OpenAI Configuration (for cloud-based models)
# Set this if you want to use OpenAI's hosted models.
OPENAI_API_KEY=your-openai-api-key-here

# Directory Configuration
# WATCH_DIRECTORY: folder to monitor for new input images/files.
# OUTPUT_DIRECTORY: where analysis results will be written.
# CHECK_INTERVAL: poll interval in seconds.
WATCH_DIRECTORY=./input_images
OUTPUT_DIRECTORY=./analysis_output
CHECK_INTERVAL=5

# Model Configuration
# If using OpenAI cloud models: gpt-4o, gpt-4-vision-preview, gpt-4-turbo
# If using a local Ollama model: prefix with "ollama/", e.g. ollama/llava
MODEL=gpt-4o

# Ollama Configuration (only needed if using a local Ollama server)
# Example: OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_BASE_URL=http://localhost:11434
